global_config = {
	# Autocomplete
	"autocomplete" : {
		# total number of search results to return
		"max_results": 5,
		# Only run autocomplete algo if the number of elements returned from
		# dynamodb is less than this value
		"max_search": 10
	}
}